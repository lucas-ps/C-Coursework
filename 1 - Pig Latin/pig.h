#ifndef PIG_H
#define PIG_H

/**
 * @brief Takes in a pointer to a word and returns a pointer to the pig latin 
 * translation of it.
 * 
 * @param word The original word
 * @return char* The pig latin translated version of the word
 */
char *pig(char *word);

#endif
